 - ReplicaSet introduction:

Nesta aula discutiremos sobre um controlador em particular o controlador de replicação,
Então, o que é uma replica e porque precisamos de um controlador de replicação?
- E se por algum motivo nosso aplicativo travar e o pod Falhar - "Pod Fail"!,
 Os usuarios não poderão mais acessar nosso aplicativo para evitar que percam
 o acesso ao nosso aplicativo? 

Gostariamos de ter mais de uma instancia ou pod em execução ao mesmo tempo,
Dessa forma se falhar, ainda teremos nosso aplicativo em execução.
Por outro lado, o controlador de replicação nos ajuda a funcionar varias instancias de
um unico POD no Kubernetes Cluster proporcionando alta disponibilidade (high availability)

-> Então isto significa que você não pode usar um controlador de replicação se planeja ter 
um unico POD?
Não...
Mesmo se você tivet um unico POD (single POD), o controlador de replicação pode
ajudar trazendo automaticamente um novo POD, quando existem falha. O controlador de replicação
garante que o numero especificado de pods esteja em execução, o tempo todo, mesmo que seja apenas 1
ou 100 PODs.

-> Load Balancer & Scaling:
Outro motivo pela qual precisamos do controlador de replicação é criar varias partes para 
compartilhar a carga entre elas.
Por exemplo, neste cenario simples, temos um Unico POD atendendo um conjunto de usuarios.
Quando o numero de usuarios aumenta, implantamos POD adicionais para equilibrar  a carga 
entre os dois PODs.

Se a Demanda aumentar ainda mais e ficarmos sem recursos, no primeiro nó (Node) perderemos
implantar peças adicionais nos outros (Nodes) do Cluster.
-> O replication Controller se estende por varios Nodes do Cluster, isto nos ajuda a equilibrar a
carga em varios PODs em Nodes diferentes, bem como dimensionar nosso aplicativo (App) Quando a 
demanda Aumentar.

Principais diferenças entre um:

Replication Controller       |         ReplicaSet

Vamos agora ver como criamos um controlador de replicação (ReplicationController)
chamaremos este arquivo de: re-definition.yml
estrutura do arquivo:



